<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dgut.collegemarket">

    <uses-permission android:name="android.permission.INTERNET" />
    <!--在SDCard中创建与删除文件权限  -->
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"/>
    <!-- 往SDCard写入数据权限 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <!--  从SDCard读取数据权限 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>


    <application
        android:allowBackup="true"
        android:icon="@mipmap/collegemarket"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/collegemarket"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.RegisterActivity" />
        <activity android:name=".activity.MainActivity" />
        <activity android:name=".activity.GoodsAddActivity" />
        <activity android:name=".activity.CheckConsumptionAndSalesRecordsActivity" />
        <activity android:name=".activity.CheckSubscriptionAndMessagesRecordsActivity" />
        <activity android:name=".activity.ContentConsumptionActivity" />
        <activity android:name=".activity.PostAddActivity" />
        <activity android:name=".activity.RechargeActivity" />
        <activity android:name=".activity.UserInfoActivity" />
        <activity android:name=".activity.SearchActivity" />
        <activity android:name=".activity.DirectMessagesActivity" />
        <activity
            android:name=".activity.GoodsContentActivity"
           ></activity>
        <activity android:name=".activity.PostAddActivity" />
        <activity android:name=".activity.PostContentActivity" />
        <activity android:name=".activity.CommentActivity"></activity>
    </application>

</manifest>